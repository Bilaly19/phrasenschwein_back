{
  "name": "phrasenschwein-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "test": "node --test",
    "test:contract": "node --test tests/contract/api.contract.test.js",
    "test:contract:security": "node --test tests/contract/security.contract.test.js",
    "lint": "node scripts/check-syntax.js"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "helmet": "^8.1.0"
  }
}
